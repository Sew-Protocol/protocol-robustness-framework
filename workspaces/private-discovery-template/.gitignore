# Private discovery workspace — gitignored by default
# Copy this template to ~/prf-private/<id>/ and work there.
# This .gitignore prevents accidental commit of sensitive material
# if someone initializes inside the repo tree.

sealed-log/
encrypted-*/
local-runs/
disclosure-candidates/
export/
*.key
*.pem
*.age
*.gpg
