# Framework + SEW Workspace

Full codebase: framework core (`src/`) + SEW protocol implementation
(`protocols_src/`).  This is equivalent to working from the project root
with the `:with-sew` alias.

  clojure -M           # PRF-only (default)
  clojure -M:with-sew  # full stack
